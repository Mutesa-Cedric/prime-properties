import React from 'react'
import { property } from '../typings'

function Property({name,price,location,description,image}:property) {
  return (
    <div>Property</div>
  )
}

export default Property